---
interface Props {
  iconName: string;
  cardTitle: string;
  linkTarget?: string;
  description?: string;
}
const DEBUG = false;

const {
  iconName = "",
  cardTitle,
  linkTarget = "",
  description = "",
} = Astro.props;

if (DEBUG) {
  console.log(`cardTitle is ${cardTitle}`);
}
import "iconify-icon";
import "@spectrum-css/tokens/dist/index.css";
import "@spectrum-css/icon/dist/index.css";
import "@spectrum-css/card/dist/index.css";
import "@styles/HorizontalCard.css";
---

<horizontal-card
  linkTarget={linkTarget}
  iconName={iconName}
  cardTitle={cardTitle}
  cardDescription={description}
>
</horizontal-card>

<script>
  import HorizontalCard from "./HorizontalCard.ts";
</script>
